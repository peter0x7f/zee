import { Tabs, Redirect } from 'expo-router';
import { useAuth, AuthProvider } from '../Contexts/AuthContext';

export default () => {
  
    return(

<Tabs>         
 </Tabs>

        
    )
    
   
}